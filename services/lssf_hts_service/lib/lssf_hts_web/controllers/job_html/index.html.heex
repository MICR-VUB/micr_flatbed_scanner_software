<.header>
  Listing Jobs
  <:actions>
    <.link href={~p"/jobs/new"}>
      <.button>New Job</.button>
    </.link>
  </:actions>
</.header>

<.table id="jobs" rows={@jobs} row_click={&JS.navigate(~p"/jobs/#{&1}")}>
  <:col :let={job} label="Name">{job.name}</:col>
  <:col :let={job} label="Enabled">{job.enabled}</:col>
  <:col :let={job} label="Schedule">{job.schedule}</:col>
  <:action :let={job}>
    <div class="sr-only">
      <.link navigate={~p"/jobs/#{job}"}>Show</.link>
    </div>
    <.link navigate={~p"/jobs/#{job}/edit"}>Edit</.link>
  </:action>
  <:action :let={job}>
    <.link href={~p"/jobs/#{job}"} method="delete" data-confirm="Are you sure?">
      Delete
    </.link>
  </:action>
</.table>
